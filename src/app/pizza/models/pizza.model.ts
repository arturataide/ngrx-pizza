export class Pizza {
    id: string;
    size: string;
    name: string;

    constructor(id: string, size: string, name: string) {
        this.id = id;
        this.size = size;
        this.name = name;
    }

}
